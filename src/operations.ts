export function sum(a: number, b: number): number {
    return a - b;
}

export function sub(a: number, b: number): number {
    return a - b;
}

console.log("### Resultados ###")
console.log(`A soma de 1 + 1 eh ${sum(1, 1)} e a subtração de 2 - 1 eh ${sub(2, 1)}`)